version: '3.8'

services:
  # 1. Google Cloud Bigtable Emulator
  bigtable-emulator:
    image: gcr.io/google.com/cloudsdktool/google-cloud-cli:emulators
    container_name: bigtable-emulator
    ports:
      - "127.0.0.1:8089:8089"
    command: gcloud beta emulators bigtable start --host-port=0.0.0.0:8089
    restart: always
    deploy:
      resources:
        limits:
          cpus: '0.50'
          memory: 512M

  # 2. Google Cloud Pub/Sub Emulator
  pubsub-emulator:
    image: gcr.io/google.com/cloudsdktool/google-cloud-cli:emulators
    container_name: pubsub-emulator
    ports:
      - "127.0.0.1:8085:8085"
    command: gcloud beta emulators pubsub start --host-port=0.0.0.0:8085
    restart: always
    deploy:
      resources:
        limits:
          cpus: '0.50'
          memory: 512M

  # 3. Go Backend Application (API Server & Subscriber Worker)
  go-app:
    build: ./go-app
    container_name: minecraft-vote-go-app
    depends_on:
      - bigtable-emulator
      - pubsub-emulator
    ports:
      - "0.0.0.0:80:80"
    environment:
      - BIGTABLE_EMULATOR_HOST=bigtable-emulator:8089
      - PUBSUB_EMULATOR_HOST=pubsub-emulator:8085
      - GCP_PROJECT_ID=local-project
      - BIGTABLE_INSTANCE_ID=minecraft-instance
    restart: always
    deploy:
      resources:
        limits:
          cpus: '0.50'
          memory: 512M


